"use client";

// VaultSidebar removed — vault flow uses clean full-screen layout.
// Keeping file to avoid import errors from any remaining references.
export function VaultSidebar() {
  return null;
}
