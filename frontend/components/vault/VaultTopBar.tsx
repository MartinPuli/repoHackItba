"use client";

// VaultTopBar removed — vault flow uses clean full-screen layout.
// Keeping file to avoid import errors from any remaining references.
export function VaultTopBar({ title: _title }: { title: string }) {
  return null;
}
